# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.egg
*.egg-info/
dist/
build/
*.spec

# Environment
.venv/
venv/
env/
.env

# Streamlit
.streamlit/secrets.toml

# Generated data cache (may contain real contract/pricing data)
data/cache/

# Tooling
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Personal Claude Code overrides (machine-specific PATH, per-user permissions)
.claude/settings.local.json

# Graphify — track the current graph (graph.json, graph.html, GRAPH_REPORT.md,
# manifest.json), but not its query cache or the dated curated-graph backups
# `graphify update` writes on every run.
graphify-out/cache/
graphify-out/[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]/

# macOS
.DS_Store
