streamlit>=1.32.0,<2.0.0
# <3.0.0 previously here didn't match what was actually installed/tested
# (pandas 3.0.3) — core/export.py's cell-sanitization fix was written to
# work across both 2.x and 3.x (Series.map, not the removed DataFrame
# .applymap), so raising the cap to match reality rather than pinning back
# to an old constraint nothing was actually enforcing.
pandas>=2.0.0,<4.0.0
psycopg[binary,pool]>=3.1.0,<4.0.0
python-dotenv>=1.0.0,<2.0.0
httpx>=0.27.0,<1.0.0
pyarrow>=15.0.0,<25.0.0
openpyxl>=3.1.0,<4.0.0
# >=12.3.0 floor: 12.2.0 and earlier carry 13 known CVEs (ASVS-AUDIT.md item 6).
Pillow>=12.3.0,<13.0.0
keyring>=25.0.0,<26.0.0
# >=42.0.0 floor was already current; installed/verified at 50.0.1, which
# fixes CVE-2026-69247 (PYSEC-2026-3552) present in the 49.0.0 this project
# had picked up transitively before this audit.
cryptography>=42.0.0,<51.0.0
# Transitive (via streamlit), pinned explicitly — see pyproject.toml's
# comment on the same two packages.
gitpython>=3.1.51,<4.0.0
starlette>=1.3.1,<2.0.0
